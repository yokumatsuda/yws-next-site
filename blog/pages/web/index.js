"use client";

import React, { useEffect, useRef, useState, createRef } from "react";
import styles from "styles/details.module.css";
import setBgVideoSources from "components/setBgVideoSources";

function WebPage() {
  /* =============================
   * 1) スライダー用ロジック
   * ============================= */

  const slides = [
    {
      title: "企業向けWebサイト",
      titleMobile: ["企業向け", "Webサイト"],
      text: "企業のブランド価値を高めるホームページを制作。採用ページやサービスサイトにも対応可能。",
      textMobile: "LP・ホームページ制作",
      buttonText: "詳しく見る",

      bgVideoMp4: "/services-img/slider-video/web-blob3.mp4",
      bgVideoWebm: "/services-img/slider-video/web-blob3.webm",
      // ✅ 黒画面対策
      poster: "/services-img/slider-video/web-blob3.webp",

      scrollTargetId: "homepageSection",
    },
    {
      title: "Webデザイン",
      text: "最新のトレンドを取り入れたデザインで、ブランドサイトやUI/UXに特化したWebサイトを制作。",
      textMobile: "最新の技術とデザイン",
      buttonText: "詳しく見る",

      bgVideoMp4: "/services-img/slider-video/web-blob1.mp4",
      bgVideoWebm: "/services-img/slider-video/web-blob1.webm",
      // ✅ 黒画面対策
      poster: "/services-img/slider-video/web-blob1.webp",

      scrollTargetId: "webDesignSection",
    },
    {
      title: "オンラインショップ構築",
      titleMobile: ["オンライン", "ショップ構築"],
      text: "ユーザーが快適に買い物できるECサイトを構築。決済・在庫管理などの機能も充実。",
      textMobile: "ECサイト・決済・在庫管理システム",
      buttonText: "詳しく見る",

      bgVideoMp4: "/services-img/slider-video/web-blob2.mp4",
      bgVideoWebm: "/services-img/slider-video/web-blob2.webm",
      // ✅ 黒画面対策
      poster: "/services-img/slider-video/web-blob2.webp",
      scrollTargetId: "ecommerceSection",
    },
  ];

  // スライダーの状態管理
  const [currentIndex, setCurrentIndex] = useState(0);

  // ✅ 初回だけ transition を無効化するフラグ
  const [isReady, setIsReady] = useState(false);

  // TSの型指定を削除した ref
  const cardRefs = useRef([]);
  const dotRefs = useRef([]);
  // ✅ 背景 video の参照（スライド数ぶん）
  const videoRefs = useRef(slides.map(() => createRef()));
  const autoScrollRef = useRef(null);

  const autoScrollDelay = 4000;

  // 背景色を変化させる用
  const bgColors = ["#FDE2E2", "#FAF7B6", "#B8F2E6"];

  // 2) 2フレーム待ってから transition有効化
  useEffect(() => {
    if (isReady) return;

    loadShowSlideDOM(0);

    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        setIsReady(true); // ここから通常アニメON
      });
    });
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [isReady]);

  useEffect(() => {
    // ✅ 初回は isReady が true になるまで触らない（初期演出を壊さない）
    if (!isReady) return;

    loadShowSlideDOM(currentIndex);

    const newIndex = (currentIndex + slides.length) % slides.length;
    const center = newIndex;
    const left = (newIndex - 1 + slides.length) % slides.length;
    const right = (newIndex + 1) % slides.length;

    // 中央/左右を先に source セット（黒画面を減らす）
    [center, left, right].forEach((idx) => {
      const v = videoRefs.current[idx]?.current;
      const s = slides[idx];
      if (!v) return;
      setBgVideoSources(
        v,
        { mp4: s.bgVideoMp4, webm: s.bgVideoWebm, poster: s.poster },
        idx === center ? "auto" : "metadata"
      );
    });

    // 再生制御：中央だけ play、他は pause
    slides.forEach((_, idx) => {
      const v = videoRefs.current[idx]?.current;
      if (!v) return;
      if (idx === center) {
        v.currentTime = 0;
        v.play().catch(() => {});
      } else {
        v.pause();
      }
    });

    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [currentIndex]);

  useEffect(() => {
    if (!isReady) return;
    startAutoScroll();
    return () => stopAutoScroll();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [isReady]);

  /** スライドカードの配置 */
  function loadShowSlideDOM(index) {
    const newIndex = (index + slides.length) % slides.length;
    const center = newIndex;
    const left = (newIndex - 1 + slides.length) % slides.length;
    const right = (newIndex + 1) % slides.length;

    // 全カードを初期化
    cardRefs.current.forEach((card) => {
      if (card) {
        card.style.opacity = "0";
        card.style.transform = "translate(-50%,-50%) scale(0.8)";
        card.style.zIndex = "0";
      }
    });

    // 中央カード
    const centerCard = cardRefs.current[center];
    if (centerCard) {
      centerCard.style.opacity = "1";
      centerCard.style.transform = "translate(-50%,-50%) scale(1)";
      centerCard.style.zIndex = "2";
    }

    // 左右のカード
    const translateValue = "120%";
    const leftCard = cardRefs.current[left];
    if (leftCard) {
      leftCard.style.opacity = "0.8";
      leftCard.style.transform = `translate(-50%,-50%) scale(0.8) translateX(-${translateValue})`;
      leftCard.style.zIndex = "1";
    }
    const rightCard = cardRefs.current[right];
    if (rightCard) {
      rightCard.style.opacity = "0.8";
      rightCard.style.transform = `translate(-50%,-50%) scale(0.8) translateX(${translateValue})`;
      rightCard.style.zIndex = "1";
    }

    // ドットを更新
    updateDots(newIndex);

    // 背景色を更新
    const wrapper = document.getElementById("my-slider1-wrapper");
    if (wrapper) {
      wrapper.style.backgroundColor = bgColors[newIndex % bgColors.length];
    }
  }

  /** ドットの外見更新 */
  function updateDots(activeIndex) {
    dotRefs.current.forEach((dot, i) => {
      if (!dot) return;
      dot.classList.remove(styles.activeDot);
      if (i === activeIndex) {
        dot.classList.add(styles.activeDot);
      }
    });
  }

  /** オートスクロール */
  function startAutoScroll() {
    stopAutoScroll();
    setCurrentIndex((prev) => prev + 1);
    const id = window.setInterval(() => {
      setCurrentIndex((prev) => prev + 1);
    }, autoScrollDelay);
    autoScrollRef.current = id;
  }

  function stopAutoScroll() {
    if (autoScrollRef.current !== null) {
      clearInterval(autoScrollRef.current);
      autoScrollRef.current = null;
    }
  }

  /** スワイプ操作（ローカル変数だと再レンダで消えるので useRef に） */
  const xDownRef = useRef(null);

  function handleTouchStart(e) {
    stopAutoScroll();
    if (e.touches && e.touches.length > 0) {
      xDownRef.current = e.touches[0].clientX;
    }
  }

  function handleTouchMove(e) {
    if (xDownRef.current === null) return;
    if (!e.touches || e.touches.length === 0) return;

    const xUp = e.touches[0].clientX;
    const xDiff = xDownRef.current - xUp;
    const threshold = 30;

    if (Math.abs(xDiff) > threshold) {
      if (xDiff > 0) {
        // 左スワイプ
        setCurrentIndex((prev) => prev + 1);
      } else {
        // 右スワイプ
        setCurrentIndex((prev) => prev - 1);
      }
      xDownRef.current = null;
    }
  }

  /** 前/次ボタン */
  function handlePrev() {
    stopAutoScroll();
    setCurrentIndex((prev) => prev - 1);
  }
  function handleNext() {
    stopAutoScroll();
    setCurrentIndex((prev) => prev + 1);
  }

  /** ドットクリック */
  function handleDotClick(i) {
    stopAutoScroll();
    setCurrentIndex(i);
  }

  /** ページ内スクロール用 (詳しく見る押下時) */
  function scrollWithOffset(id, offset = 10) {
    const el = document.getElementById(id);
    if (!el) return;
    const y = el.getBoundingClientRect().top + window.scrollY;
    const yOffset = y - offset;
    window.scrollTo({ top: yOffset, behavior: "smooth" });
  }

  /* =============================
   * 2) アコーディオン（カテゴリ別）
   * ============================= */

  /* --- カテゴリA: 企業向けWebサイト制作 --- */
  const accordionItemsDX = [
    {
      title: "企業ブランディング＆コーポレートサイト制作",
      subtitle:
        "企業の信頼性を高めるために、デザイン性・機能性・ユーザビリティを重視したプロフェッショナルなコーポレートサイトを制作します。",
      desc: `■ サービス概要
- 企業のブランド価値を高める、洗練されたコーポレートサイトを制作
- 会社案内・事業紹介・採用情報など、目的に応じたページ設計を提供
- SEO対策・高速表示対応により、検索エンジンでの可視性を向上
- 企業の信頼性を高めるデザインとユーザビリティを考慮した設計

■ 導入メリット
- **ブランドイメージの向上**：一貫したデザイン・メッセージで企業価値を強化
- **集客力の向上**：SEO対策とSNS連携により、認知度・訪問数を増加
- **モバイル最適化**：スマホ・タブレット対応で、あらゆるデバイスで最適な表示を提供
- **運用のしやすさ**：CMS（WordPress / Headless CMS）を導入し、簡単に情報更新が可能

■ 取り扱うサービス
- **コーポレートサイト制作**（会社情報・事業紹介・企業ビジョンを伝えるWebサイト）
- **採用ページ・リクルートサイト制作**（求職者に向けた魅力的な採用情報の発信）
- **ブランディングサイト制作**（企業のコンセプトを伝える独自性のあるデザイン）

■ システム概要（技術情報）
- **フロントエンド**：React / Next.js（モダンで直感的なUI設計）
- **バックエンド**：Node.js / PHP（WordPress / Headless CMS を活用）
- **SEO対策・高速表示**：検索エンジン最適化 / 画像圧縮・キャッシュ対応
- **クラウド基盤**：AWS / Google Cloud（高可用性と拡張性を確保）

■ 導入事例
1️⃣ 「企業ブランドを強化するコーポレートサイトを制作し、認知度を向上」  
2️⃣ 「採用ページをリニューアルし、応募率の向上を実現」  
3️⃣ 「WordPressを導入し、簡単に更新できる企業サイトを構築」  

**📢 まずは無料相談から！**  
貴社のブランド価値を高める最適なWebサイトを提案いたします。

      `,
      image: "/services-img/web/Web-img4.jpeg",
    },
    {
      title: "採用・リクルート向けWebサイト制作",
      subtitle:
        "求職者に企業の魅力を伝え、採用活動を強化するために、ブランディングとエンゲージメントを重視した採用ページ・リクルートサイトを制作します。",
      desc: `
      ■ サービス概要
- 企業の採用活動を強化するため、求職者に企業の魅力を伝える採用サイトを制作
- 企業のビジョン・社風・働く環境を魅力的に表現し、エンゲージメントを向上
- エントリーフォーム・求人情報管理システムを導入し、応募の利便性を向上
- SEO対策・SNS連携を活用し、求職者へのアプローチを最適化

■ 導入メリット
- **採用ブランディングの強化**：企業の文化や働き方を魅力的に発信し、理想の人材を惹きつける
- **応募率の向上**：エントリーフォームの最適化により、求職者の離脱を防ぎスムーズな応募を実現
- **更新のしやすさ**：CMS（WordPress / Headless CMS）を導入し、求人情報を簡単に更新可能
- **集客力の向上**：SEO・SNS連携により、求職者の流入を最大化

■ 取り扱うサービス
- **採用専用サイト制作**（企業の採用情報を発信するリクルートページ）
- **エントリーフォーム・応募管理システム**（オンライン応募機能を実装）
- **求人情報管理システム**（社内で簡単に求人情報を更新できる仕組み）

■ システム概要（技術情報）
- **フロントエンド**：React / Next.js（直感的なUI設計）
- **バックエンド**：Node.js / PHP（WordPress / Headless CMS を活用）
- **SEO対策・高速表示**：検索エンジン最適化 / 画像圧縮・キャッシュ対応
- **クラウド基盤**：AWS / Google Cloud（高可用性と拡張性を確保）

■ 導入事例
1️⃣ 「企業の採用ブランドを強化し、エントリー数を向上」  
2️⃣ 「エントリーフォームを最適化し、応募者の利便性を向上」  
3️⃣ 「求人情報を簡単に更新できるシステムを導入し、運用負担を軽減」  

**📢 まずは無料相談から！**  
企業の採用力を向上させるリクルートサイトの制作をサポートします。

      `,
      image: "/services-img/web/Web-img20.jpeg",
    },
    {
      title: "サービス・プロダクトサイト制作",
      subtitle:
        "ビジネスの成長につながる集客力のあるホームページを構築し、企業のオンラインプレゼンスを最大化します。",
      desc: `
     ■ サービス概要
- 企業のサービスやプロダクトを魅力的に伝えるWebサイトを制作
- 商品・サービスの特徴や価値を明確に表現し、集客・販売につなげるデザイン設計
- SEO対策・コンテンツマーケティングを活用し、オンラインプレゼンスを最大化
- 問い合わせ・資料請求・購入ページへのスムーズな導線設計を提供

■ 導入メリット
- **集客力の向上**：SEO・SNSマーケティングを活用し、ターゲットユーザーの流入を促進
- **コンバージョン率の向上**：問い合わせ・購入への導線を最適化し、ビジネス成果を最大化
- **ブランド認知の強化**：ビジュアル・コンテンツを通じて、企業の魅力を最大限に伝える
- **モバイル最適化**：スマホ・タブレットでも快適に閲覧できるレスポンシブデザインを採用

■ 取り扱うサービス
- **サービス紹介サイト制作**（企業の提供するサービスを魅力的に伝えるWebサイト）
- **プロダクトランディングページ制作**（商品の特長を引き出し、購買促進につなげるLP制作）
- **コンテンツマーケティング支援**（SEO記事・ブログ・動画コンテンツの活用）

■ システム概要（技術情報）
- **フロントエンド**：React / Next.js（高速・モダンなUI設計）
- **バックエンド**：Node.js / WordPress（管理しやすいCMS対応）
- **SEO対策・マーケティング**：Google検索最適化・サイト高速化
- **クラウド基盤**：AWS / Google Cloud（セキュアで拡張性のあるインフラ）

■ 導入事例
1️⃣ 「企業のサービス紹介サイトを制作し、問い合わせ数を向上」  
2️⃣ 「商品の魅力を最大限に引き出すランディングページを構築し、コンバージョン率を改善」  
3️⃣ 「SEO対策を施し、ターゲットユーザーの流入を大幅に増加」  

**📢 まずは無料相談から！**  
貴社のサービス・プロダクトを最大限に引き出すWebサイト制作をサポートします。
`,
      image: "/services-img/web/Web-img17.jpeg",
    },
  ];

  // ステート: どのパネルを開いているか (DX)
  const [openIndexDX, setOpenIndexDX] = useState(null);

  function handleToggleDX(index) {
    // 同じパネル => 閉じる
    if (openIndexDX === index) {
      setOpenIndexDX(null);
      return;
    }

    // 別パネルを開こうとした場合
    if (openIndexDX !== null) {
      // 既に開いているパネルを閉じる
      const nextIndex = index;
      setOpenIndexDX(null);

      // 閉じアニメ時間: 0.4s => 500ms余裕
      setTimeout(() => {
        // スクロール先 = "dx-header-<nextIndex>"
        const targetHeaderId = `dx-header-${nextIndex}`;
        const targetEl = document.getElementById(targetHeaderId);
        if (targetEl) {
          targetEl.scrollIntoView({ behavior: "smooth", block: "start" });
        }

        // スクロール完了待ち: 300ms後に新パネルを開く
        setTimeout(() => {
          setOpenIndexDX(nextIndex);
        }, 300);
      }, 500);
    } else {
      // 何も開いていない => すぐ開く
      setOpenIndexDX(index);
    }
  }

  /* --- カテゴリB: クリエイティブWebデザイン --- */
  const accordionItemsEfficiency = [
    {
      title: "ブランドデザイン・ビジュアルアイデンティティ",
      subtitle:
        "企業やサービスのブランド価値を最大化し、視覚的に一貫性のあるデザインを提供。ロゴ・カラースキーム・フォント・Webデザインを統一し、ブランドの印象を強化します。",
      desc: `
      ■ サービス概要
- 企業やサービスのブランド価値を最大化する、統一感のあるビジュアルデザインを提供
- ロゴ・カラースキーム・フォント・Webデザインを統一し、ブランドイメージを強化
- Webサイト・印刷物・デジタルコンテンツなど、複数のメディアで一貫したデザインを適用
- ユーザーの記憶に残るブランドデザインを制作し、競争力を向上

■ 導入メリット
- **ブランドの一貫性を確保**：ロゴ・フォント・カラーの統一で、視覚的な印象を最適化
- **企業イメージの向上**：プロフェッショナルなデザインで、信頼性とブランド価値を強化
- **顧客の認知度向上**：ターゲットユーザーに印象を残し、ブランドの認知度を向上
- **多メディア対応**：Web・SNS・印刷物など、幅広いメディアに適応可能

■ 取り扱うサービス
- **ブランドガイドライン制作**（ロゴ・カラーパレット・フォントの統一ルール策定）
- **Web・SNSデザイン**（企業サイト・SNS投稿用テンプレートのデザイン）
- **印刷物・デジタル広告デザイン**（パンフレット・バナー・LPデザイン）

■ システム概要（技術情報）
- **デザインツール**：Figma / Photoshop / Adobe Firefly / Canva
- **Webデザイン**：React / Next.js / Studioエディターを活用したモダンデザイン
- **クラウドストレージ**：Google Drive / AWS S3（デザインデータの管理・共有）
- **フォーマット対応**：Web用（PNG / SVG）、印刷用（PDF）に対応

■ 導入事例
1️⃣ 「企業のブランドガイドラインを策定し、ロゴ・カラー・フォントを統一」  
2️⃣ 「Web・SNSデザインを統一し、ブランド認知度を向上」  
3️⃣ 「企業のプロモーション用パンフレットとWebバナーをデザインし、一貫性を確保」  

**📢 まずは無料相談から！**  
貴社のブランド価値を最大化するデザイン戦略を提供します。`,
      image: "/services-img/web/Web-img2.jpeg",
    },
    {
      title: "UI/UXデザイン・ユーザー体験の最適化",
      subtitle:
        "ユーザーが直感的に操作できるデザイン設計を実現し、コンバージョン向上と使いやすさを両立する最適なUI/UXを提供します。",
      desc: `
    ■ サービス概要
- 直感的な操作が可能なUI設計と、ユーザー行動を分析したUX最適化を提供
- ユーザーテスト・A/Bテストを活用し、データに基づいたデザイン改善を実施
- サイトのナビゲーション・情報設計を整理し、ユーザーの利便性を向上
- スマホ・タブレット・PCの各デバイスで最適なユーザー体験を提供

■ 導入メリット
- **ユーザーエンゲージメント向上**：快適な操作性とデザインで、滞在時間・回遊率を向上
- **コンバージョン率の改善**：フォーム・CTA・動線を最適化し、売上や問い合わせ数を増加
- **視認性・使いやすさの向上**：フォント・カラー・レイアウトを最適化し、ストレスのないUXを提供
- **モバイル最適化**：レスポンシブデザインで、どのデバイスでも最適な表示を実現

■ 取り扱うサービス
- **UIデザイン改善**（サイト・アプリのデザイン最適化、視認性向上）
- **ユーザビリティテスト**（ユーザーテスト・A/Bテストによる改善提案）
- **レスポンシブデザイン・スマホ最適化**（各デバイスでの最適表示対応）

■ システム概要（技術情報）
- **デザインツール**：Figma / Adobe Firefly / Photoshop / Canva
- **フロントエンド**：React / Next.js（高速・モダンなUI設計）
- **ユーザビリティ分析**：Google Analytics
- **クラウド対応**：AWS / Google Cloud（拡張性のあるデザイン管理）

■ 導入事例
1️⃣ 「ECサイトのUI/UXを改善し、購入完了率を20%向上」  
2️⃣ 「ナビゲーションを最適化し、ユーザーの回遊率を改善」  
3️⃣ 「A/Bテストを実施し、問い合わせフォームのCV率を向上」  

**📢 まずは無料相談から！**  
コンバージョンを高め、使いやすいWebデザインを提供します。`,
      image: "/services-img/web/Web-img15.jpeg",
    },
    {
      title: "クリエイティブコンテンツ制作",
      subtitle:
        "ブランドイメージを強化し、視覚的に魅力的なコンテンツを制作。Web・SNS・印刷物など、あらゆるメディアに対応したデザインコンテンツを提供します。",
      desc: `
     ■ サービス概要
- 企業のブランドイメージを高めるクリエイティブコンテンツを制作
- Webサイト・SNS・広告・印刷物など、多様なメディアに対応したデザインを提供
- AIを活用した画像生成・編集により、オリジナルで高品質なコンテンツをスピーディに制作
- インタラクティブデザインやアニメーションを活用し、ユーザーエンゲージメントを向上

■ 導入メリット
- **ブランドの視認性向上**：統一感のあるデザインで、企業の印象を強化
- **コンテンツの魅力最大化**：プロフェッショナルなデザインで、顧客の関心を引きつける
- **多メディア対応**：Web・SNS・動画・パンフレット・広告など、幅広い活用が可能
- **効率的な制作プロセス**：AIやデザインツールを活用し、短納期で高品質なコンテンツを提供

■ 取り扱うサービス
- **Web用ビジュアルコンテンツ制作**（Webサイト向けの高品質な画像・動画・アニメーション）
- **SNS・デジタル広告デザイン**（Instagram・Twitter・Facebook・YouTube広告のクリエイティブ制作）
- **印刷物・パンフレットデザイン**（会社案内・チラシ・カタログ・名刺デザイン）

■ システム概要（技術情報）
- **デザインツール**：Figma / Photoshop / Adobe Firefly / Canva
- **Webデザイン**：React / Next.js（モダンで直感的なUI設計）
- **動画・アニメーション制作**：SoraAI を活用した動画生成・編集
- **クラウド管理**：Google Drive / AWS S3（デザインデータの保存・管理）

■ 導入事例
1️⃣ 「企業のSNS広告クリエイティブを統一し、ブランドイメージを強化」  
2️⃣ 「Webサイト向けのビジュアルコンテンツを制作し、ユーザーエンゲージメントを向上」  
3️⃣ 「プロモーション用パンフレットとデジタル広告を連携し、販促効果を最大化」  

**📢 まずは無料相談から！**  
貴社のブランド価値を最大化するクリエイティブコンテンツを提供します。`,
      image: "/services-img/web/Web-img14.jpeg",
    },
  ];

  const [openIndexEfficiency, setOpenIndexEfficiency] = useState(null);

  function handleToggleEfficiency(index) {
    if (openIndexEfficiency === index) {
      setOpenIndexEfficiency(null);
      return;
    }

    if (openIndexEfficiency !== null) {
      const nextIndex = index;
      setOpenIndexEfficiency(null);

      // 閉じてからスクロール → 開く (同じパターン)
      setTimeout(() => {
        const targetId = `efficiency-header-${nextIndex}`;
        const el = document.getElementById(targetId);
        if (el) {
          el.scrollIntoView({ behavior: "smooth", block: "start" });
        }
        setTimeout(() => {
          setOpenIndexEfficiency(nextIndex);
        }, 300);
      }, 500);
    } else {
      setOpenIndexEfficiency(index);
    }
  }

  /* --- カテゴリC: ECサイト・オンラインショップ構築 --- */
  const accordionItemsPaperless = [
    {
      title: "ECサイト開発・オリジナルシステム構築",
      subtitle:
        "ユーザー体験を重視したECサイトを開発し、決済・在庫管理・会員機能など、ビジネスに最適なオリジナルECシステムを構築します。",
      desc: `■ サービス概要
- ユーザーの利便性を最大限に考慮したECサイトを開発し、売上向上をサポート
- WooCommerceやReact / Next.jsを活用し、スケーラブルなECサイトを構築
- 商品管理・決済・会員システムを含むオリジナルECシステムの開発
- SEO対策・サイト高速化を施し、集客力とコンバージョン率を向上

■ 導入メリット
- **柔軟なカスタマイズ**：ビジネスモデルに応じた機能を自由に追加・最適化
- **決済・在庫管理の効率化**：リアルタイムでの在庫管理・多様な決済手段に対応
- **モバイル最適化**：スマホ・タブレットでもスムーズに利用できるECサイトを提供
- **集客力向上**：SEO・SNSマーケティング施策を活用し、売上の最大化を支援

■ 取り扱うサービス
- **ECサイト構築（WooCommerce / オリジナル開発）**（柔軟なカスタマイズ可能なECサイトの開発）
- **決済・カートシステムの導入**（Stripe / PayPal / クレジットカード対応）
- **在庫管理・物流システムの統合**（リアルタイム在庫管理・発送管理機能）

■ システム概要（技術情報）
- **フロントエンド**：React / Next.js（高速で直感的なUI設計）
- **バックエンド**：Node.js / PHP（カスタマイズ可能なEC機能）
- **データベース**：MySQL（商品・注文データの管理）
- **クラウド基盤**：AWS / Google Cloud（スケーラブルなインフラ構築）

■ 導入事例
1️⃣ 「WooCommerceを活用し、多機能なECサイトを構築」  
2️⃣ 「オリジナルECシステムを開発し、決済・在庫管理を最適化」  
3️⃣ 「SEO対策と高速化を実施し、売上・コンバージョン率を向上」  

**📢 まずは無料相談から！**  
貴社のビジネスに最適なECサイトを構築し、売上の最大化をサポートします。 `,
      image: "/services-img/web/Web-img10.jpeg",
    },
    {
      title: "決済・在庫管理・システム連携",
      subtitle:
        "ECサイトの決済・在庫管理を効率化し、業務システムとのシームレスな連携を実現。スムーズな運営をサポートし、売上最大化を支援します。",
      desc: `
      ■ サービス概要
- クレジットカード・QRコード・銀行振込など、さまざまな決済手段を導入し、スムーズな購入フローを実現
- 在庫管理システムを構築し、リアルタイムでの在庫更新・発注・物流管理を最適化
- クラウド環境を活用し、スケーラブルなシステムで安定運用を実現

■ 導入メリット
- **決済フローの最適化**：多様な決済手段に対応し、ユーザーの利便性を向上
- **在庫管理の自動化**：リアルタイムの在庫同期で、欠品・過剰在庫を防止
- **クラウド対応**：AWS・Google Cloudを活用し、安定した運用環境を提供

■ 取り扱うサービス
- **決済システム導入（Stripe / PayPal / Amazon Pay）**（ECサイト・店舗向け決済システム）
- **在庫管理システムの構築**（リアルタイム在庫同期・受発注管理）

■ システム概要（技術情報）
- **フロントエンド**：React / Next.js（直感的なUIでスムーズな管理画面を提供）
- **バックエンド**：Node.js / PHP（決済・在庫データの管理と処理）
- **データベース**：MySQL / PostgreSQL（販売・受注・在庫データの管理）
- **クラウド基盤**：AWS / Google Cloud（スケーラブルなEC運営環境）

■ 導入事例
1️⃣ 「ECサイトに複数の決済手段を導入し、購入率を向上」  
2️⃣ 「リアルタイムの在庫管理システムを構築し、業務効率を大幅に改善」  
3️⃣ 「ECサイトと在庫管理システムを連携し、販売データの管理を効率化」  

**📢 まずは無料相談から！**  
ECサイト運営を効率化し、売上を最大化する決済・在庫管理システムを構築します。 `,
      image: "/services-img/web/Web-img13.jpeg",
    },
    {
      title: "集客・売上向上のマーケティング支援",
      subtitle:
        "SEO・SNSマーケティング・広告運用・データ分析を活用し、集客力を強化。ターゲットにリーチし、売上向上をサポートします。",
      desc: `■ サービス概要
- SEO・コンテンツマーケティングを活用し、検索エンジンからの流入を最大化
- Google広告・SNS広告の運用支援で、ターゲットユーザーへ効果的にアプローチ
- メールマーケティング・リターゲティング施策により、リピーター獲得を促進

■ 導入メリット
- **集客力の向上**：SEO対策・広告運用により、新規顧客の流入を増加
- **売上・コンバージョン最適化**：ユーザー行動を分析し、サイト改善を実施
- **リピーター獲得の強化**：メールマーケティング・ポイント制度で顧客エンゲージメントを向上
- **SNS・Web連携**：SNS広告・Webコンテンツを組み合わせ、認知度を向上

■ 取り扱うサービス
- **SEO・コンテンツマーケティング**（Google検索の上位表示対策・ブログ記事作成）
- **SNS広告・Google広告運用支援**（ターゲット広告・リターゲティング施策）
- **データ分析・サイト改善**（Google Analytics）

■ システム概要（技術情報）
- **SEO最適化**：Google検索アルゴリズムに対応したサイト設計
- **広告運用ツール**：Google Ads
- **データ分析**：Google Analytics
- **マーケティング自動化**：メール配信・顧客リスト管理

■ 導入事例
1️⃣ 「SEO対策を施し、検索エンジンからの流入を2倍に増加」  
2️⃣ 「SNS広告を活用し、ターゲットユーザーへのリーチを拡大」  
3️⃣ 「データ分析をもとにUI/UXを改善し、コンバージョン率を向上」  

**📢 まずは無料相談から！**  
効果的なマーケティング施策で、集客力と売上の最大化をサポートします。`,
      image: "/services-img/web/Web-img9.jpeg",
    },
  ];

  const [openIndexPaperless, setOpenIndexPaperless] = useState(null);

  function handleTogglePaperless(index) {
    if (openIndexPaperless === index) {
      setOpenIndexPaperless(null);
      return;
    }

    if (openIndexPaperless !== null) {
      const nextIndex = index;
      setOpenIndexPaperless(null);

      setTimeout(() => {
        const targetId = `paperless-header-${nextIndex}`;
        const el = document.getElementById(targetId);
        if (el) {
          el.scrollIntoView({ behavior: "smooth", block: "start" });
        }
        setTimeout(() => {
          setOpenIndexPaperless(nextIndex);
        }, 300);
      }, 500);
    } else {
      setOpenIndexPaperless(index);
    }
  }

  /* =============================
   * 3) JSX レンダリング
   * ============================= */
  return (
    <>
      {/* ========== スライダーエリア ========== */}
      <div id="my-slider1-wrapper" className={styles.Container}>
        <div
          className={styles.mySlider1Container}
          onTouchStart={handleTouchStart}
          onTouchMove={handleTouchMove}
        >
          {slides.map((slide, i) => (
            <div
              key={i}
              className={`${styles.mySlider1Card} ${isReady ? styles.ready : styles.notReady}`}
              ref={(el) => {
                if (el) cardRefs.current[i] = el;
              }}
            >
              <video
                ref={videoRefs.current[i]}
                className={styles.bgVideo}
                autoPlay
                muted
                loop
                playsInline
                preload={i === 0 ? "auto" : "metadata"}
                poster={slide.poster}
              >
                <source src={slide.bgVideoWebm} type="video/webm" />
                <source src={slide.bgVideoMp4} type="video/mp4" />
              </video>

              <div className={styles.mySlider1CardContent}>
                {/* PC用タイトル */}
                <h2 className={styles.slideTitleDesktop}>{slide.title}</h2>

                {/* モバイル用タイトル（配列なら改行して表示） */}
                <h2 className={styles.slideTitleMobile}>
                  {Array.isArray(slide.titleMobile)
                    ? slide.titleMobile.map((line, idx) => (
                        <React.Fragment key={idx}>
                          {line}
                          {idx !== slide.titleMobile.length - 1 && <br />}
                        </React.Fragment>
                      ))
                    : slide.titleMobile ?? slide.title}
                </h2>

                {/* ✅ PC用（長い文） */}
                <p className={styles.slideTextDesktop}>
                  {slide.text.split("\n").map((part, idx) => (
                    <React.Fragment key={idx}>
                      {part}
                      <br />
                    </React.Fragment>
                  ))}
                </p>

                <p className={styles.slideTextMobile}>
                  {(slide.textMobile ?? slide.text).split("\n").map((part, idx) => (
                    <React.Fragment key={idx}>
                      {part}
                      <br />
                    </React.Fragment>
                  ))}
                </p>

                {slide.buttonText && slide.scrollTargetId && (
                  <button
                    className={styles.cardButton}
                    onClick={() => scrollWithOffset(slide.scrollTargetId, 80)}
                  >
                    {slide.buttonText}
                  </button>
                )}
              </div>
            </div>
          ))}

          {/* 前・次ボタン */}
          <button className={styles.mySlider1Prev} onClick={handlePrev}>
            &lt;
          </button>
          <button className={styles.mySlider1Next} onClick={handleNext}>
            &gt;
          </button>
        </div>

        {/* ドットナビ */}
        <div className={styles.mySlider1Dots}>
          {slides.map((_, i) => (
            <span
              key={i}
              className={styles.mySlider1Dot}
              ref={(el) => {
                if (el) dotRefs.current[i] = el;
              }}
              onClick={() => handleDotClick(i)}
            />
          ))}
        </div>

        {/* Play/Stop ボタン */}
        <div className={styles.mySlider1Controls}>
          <button onClick={startAutoScroll}>Play</button>
          <button onClick={stopAutoScroll}>Stop</button>
        </div>
      </div>

      {/* ========== アコーディオンエリア ========== */}
      <section className={styles.extraSection}>
        <div className={styles.categoriesWrapper}>
          {/* === カテゴリA: 企業向けWebサイト制作 === */}
          <div className={styles.categoryBlock}>
            <h2 id="homepageSection" className={styles.categoryTitle}>
              企業向けWebサイト制作
            </h2>

            {accordionItemsDX.map((item, idx) => {
              const isOpen = openIndexDX === idx;
              return (
                <div key={idx} className={styles.accordionItem}>
                  <div
                    id={`dx-header-${idx}`} // ← ここでIDを設定
                    className={styles.accordionHeader}
                    onClick={() => handleToggleDX(idx)}
                  >
                    <div>
                      <h3 className={styles.cardTitle}>{item.title}</h3>
                      <span className={styles.cardLabel}>{item.subtitle}</span>
                    </div>

                    <span className={`${styles.accordionArrow} ${isOpen ? styles.open : ""}`}>
                      ▼
                    </span>
                  </div>

                  <div className={`${styles.accordionContent} ${isOpen ? styles.open : ""}`}>
                    <div className={styles.accordionContentInner}>
                      {/* 左: テキスト */}
                      <div className={styles.cardTextArea}>
                        <p className={styles.cardDesc}>{item.desc}</p>
                        <div className={styles.buttonRow}>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/contact")}
                          >
                            お問い合わせ
                          </button>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/use")}
                          >
                            ご利用の流れ
                          </button>
                        </div>
                      </div>

                      {/* 右: 画像 */}
                      <div className={styles.cardImageWrapper}>
                        <img src={item.image} alt={item.title} className={styles.cardImage} />
                      </div>
                    </div>
                  </div>
                </div>
              );
            })}
          </div>

          {/* === カテゴリB: クリエイティブWebデザイン === */}
          <div className={styles.categoryBlock}>
            <h2 id="webDesignSection" className={styles.categoryTitle}>
              クリエイティブWebデザイン
            </h2>

            {accordionItemsEfficiency.map((item, idx) => {
              const isOpen = openIndexEfficiency === idx;
              return (
                <div key={idx} className={styles.accordionItem}>
                  <div
                    id={`efficiency-header-${idx}`}
                    className={styles.accordionHeader}
                    onClick={() => handleToggleEfficiency(idx)}
                  >
                    <div>
                      <h3 className={styles.cardTitle}>{item.title}</h3>
                      <span className={styles.cardLabel}>{item.subtitle}</span>
                    </div>

                    <span className={`${styles.accordionArrow} ${isOpen ? styles.open : ""}`}>
                      ▼
                    </span>
                  </div>

                  <div className={`${styles.accordionContent} ${isOpen ? styles.open : ""}`}>
                    <div className={styles.accordionContentInner}>
                      <div className={styles.cardTextArea}>
                        <p className={styles.cardDesc}>{item.desc}</p>
                        <div className={styles.buttonRow}>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/contact")}
                          >
                            お問い合わせ
                          </button>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/use")}
                          >
                            ご利用の流れ
                          </button>
                        </div>
                      </div>

                      <div className={styles.cardImageWrapper}>
                        <img src={item.image} alt={item.title} className={styles.cardImage} />
                      </div>
                    </div>
                  </div>
                </div>
              );
            })}
          </div>

          {/* === カテゴリC: ECサイト・オンラインショップ構築 === */}
          <div className={styles.categoryBlock}>
            <h2 id="ecommerceSection" className={styles.categoryTitle}>
              ECサイト・オンラインショップ構築
            </h2>

            {accordionItemsPaperless.map((item, idx) => {
              const isOpen = openIndexPaperless === idx;
              return (
                <div key={idx} className={styles.accordionItem}>
                  <div
                    id={`paperless-header-${idx}`}
                    className={styles.accordionHeader}
                    onClick={() => handleTogglePaperless(idx)}
                  >
                    <div>
                      <h3 className={styles.cardTitle}>{item.title}</h3>
                      <span className={styles.cardLabel}>{item.subtitle}</span>
                    </div>

                    <span className={`${styles.accordionArrow} ${isOpen ? styles.open : ""}`}>
                      ▼
                    </span>
                  </div>

                  <div className={`${styles.accordionContent} ${isOpen ? styles.open : ""}`}>
                    <div className={styles.accordionContentInner}>
                      <div className={styles.cardTextArea}>
                        <p className={styles.cardDesc}>{item.desc}</p>
                        <div className={styles.buttonRow}>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/contact")}
                          >
                            お問い合わせ
                          </button>
                          <button
                            className={styles.linkButton}
                            onClick={() => (window.location.href = "/use")}
                          >
                            ご利用の流れ
                          </button>
                        </div>
                      </div>

                      <div className={styles.cardImageWrapper}>
                        <img src={item.image} alt={item.title} className={styles.cardImage} />
                      </div>
                    </div>
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </section>
    </>
  );
}

export default WebPage;
